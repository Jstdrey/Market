# Server timezone (used inside container)
TZ=Europe/Moscow

# Streamlit port inside the container
STREAMLIT_SERVER_PORT=8501

# Exposed port on the host
MARKET_PUBLIC_PORT=8501

# Optional image tag
MARKET_IMAGE_TAG=latest

# Persistent runtime directory (absolute path recommended on server)
MARKET_RUNTIME_DIR=./runtime
